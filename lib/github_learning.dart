import 'package:flutter/material.dart';

class GitHubLearning extends StatelessWidget {
  const GitHubLearning({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Github learning'),
      ),
      body: const Column(),
    );
  }
}
